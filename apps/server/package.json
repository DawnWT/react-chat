{
  "name": "server",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "dev": "rm -rf dist && tsc && node --env-file=.env.local dist/main.js",
    "migrate": "rm -rf dist && tsc && node --env-file=.env.local dist/migrate.js",
    "build": "rm -rf dist && tsc",
    "start": "node --env-file=.env.local dist/main.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.7.0",
    "@hono/zod-validator": "^0.1.11",
    "hono": "^3.12.8",
    "kysely": "^0.27.2",
    "pg": "^8.11.3",
    "socket.io": "^4.7.4",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0"
  }
}
